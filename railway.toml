[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/mcp/Dockerfile"

[deploy]
startCommand = "node dist/http-entry.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
